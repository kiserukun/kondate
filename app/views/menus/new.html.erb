<h3>投稿する</h3>
    <%= form_with(model: @menu, local: true) do |f| %>
      <%= f.text_field :name, placeholder: "Nickname" %>

    
       <%#日曜日%>
      <%= f.collection_select(:sokuseki_id, Sokuseki.all, :id, :name, {}, {class:"genre-select"}) %>
      <%= f.text_area :sun_making, placeholder: "text", rows: "2" %></p>

      <%#月曜日%>
      <%= f.collection_select(:sokuseki_mon_id, Sokuseki.all, :id, :name, {}, {class:"genre-select"}) %>
      <%= f.text_area :mon_making, placeholder: "text", rows: "2" %></p>

      <%#火曜日%>
      <%= f.collection_select(:sokuseki_tue_id, Sokuseki.all, :id, :name, {}, {class:"genre-select"}) %>
      <%= f.text_area :tue_making, placeholder: "text", rows: "2" %></p>

      <%#水曜日%>
      <%= f.collection_select(:sokuseki_wed_id, Sokuseki.all, :id, :name, {}, {class:"genre-select"}) %>
      <%= f.text_area :wed_making, placeholder: "text", rows: "2" %></p>
     
      <%#木曜日%>
      <%= f.collection_select(:sokuseki_thu_id, Sokuseki.all, :id, :name, {}, {class:"genre-select"}) %>
      <%= f.text_area :thu_making, placeholder: "text", rows: "2" %></p>

      <%#金曜日%>
      <%= f.collection_select(:sokuseki_fri_id, Sokuseki.all, :id, :name, {}, {class:"genre-select"}) %>
      <%= f.text_area :fri_making, placeholder: "text", rows: "2" %></p>
      
      <%#土曜日%>
      <%= f.collection_select(:sokuseki_sat_id, Sokuseki.all, :id, :name, {}, {class:"genre-select"}) %>
      <%= f.text_area :sat_making, placeholder: "text", rows: "2" %></p>
      <%= f.submit "SEND" %>
    <% end %>